# Local archive

Historical snapshots and reference source archives are kept here locally for research provenance. Archive binaries are intentionally excluded from Git because they are not needed to run the active implementation.

